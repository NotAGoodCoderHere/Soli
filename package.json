{
  "name": "soliteir",
  "version": "0.0.1",
  "description": "the soliteir card game",
  "main": "app.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "run": "node app.js"
  },
  "dependencies": {
    "body-parser": "^2.2.0",
    "express": "^5.1.0",
    "sha256": "^0.2.0"
  }
}
